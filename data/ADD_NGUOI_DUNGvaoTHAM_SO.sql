--Chạy từng dòng theo khoản cách t đã đặt nhé
USE QLCHNongDuoc;
GO

ALTER TABLE THAM_SO
ADD NGUOI_DUNG BIGINT NULL;

DECLARE @MaxID BIGINT;
SELECT @MaxID = ISNULL(MAX(ID), 0) + 1 FROM NGUOI_DUNG;
UPDATE THAM_SO SET NGUOI_DUNG = @MaxID;

ALTER TABLE THAM_SO
ALTER COLUMN NGUOI_DUNG BIGINT NOT NULL;

SELECT NGUOI_DUNG FROM THAM_SO;
